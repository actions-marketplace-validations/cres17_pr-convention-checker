# Drift Gate — 코드 리뷰 체크리스트

> **사용 목적**: 이 문서는 코드 리뷰, 구현 검증, 문서-코드 정합성 확인을 위한 실사용 체크리스트입니다.
> "구현됐다"는 주장이 있으면 아래 검증 기준으로 직접 확인하세요.

---

## 이 문서의 구성

| 섹션 | 목적 |
|------|------|
| [1. 체크박스 의미 기준](#1-체크박스-의미-기준) | 체크 방법과 판정 규칙 |
| [2. 실제 구현 검증 항목](#2-실제-구현-검증-항목) | 코드에서 직접 확인해야 할 것들 |
| [3. 문서-코드 정합성](#3-문서-코드-정합성) | README와 코드 불일치 여부 |
| [4. 비용·성능 수치 검증](#4-비용성능-수치-검증) | 토큰 절감 수치가 실제인지 확인 |
| [5. 코드 품질 리뷰](#5-코드-품질-리뷰) | 설계·테스트·안전성 |
| [6. 미완료 / 부분 구현](#6-미완료--부분-구현) | 아직 동작하지 않는 기능 목록 |

---

## 1. 체크박스 의미 기준

- `[x]` **검증됨**: 아래 조건 중 하나 이상 충족
  - CLI 명령 실행 → 실제 출력 확인
  - `py -m pytest drift_gate/tests/ -q` 통과
  - 코드에서 직접 해당 로직 확인
- `[ ]` **미확인 또는 미구현**: 코드/테스트로 동작을 증명하지 못한 것
- **`부분 구현`**: 체크하지 않고 현재 범위와 남은 작업을 명시

> 이전 버전에서 근거 없이 체크된 항목들은 모두 `[ ]`로 초기화했습니다.
> 아래 항목을 직접 실행해서 확인한 후 `[x]`로 전환하세요.

---

## 2. 실제 구현 검증 항목

### 2-1. CLI 명령 동작

아래 명령이 실제로 실행되고 의미 있는 출력을 내는지 확인:

| 명령 | 검증 방법 | 상태 |
|------|-----------|------|
| `py main.py check` | 로컬 diff 분석 후 결과 출력 | `[x]` 확인 (`doctor` 출력에서 48 files diff) |
| `py main.py doctor` | OK/WARN 체크리스트 출력 | `[x]` 확인 |
| `py main.py demo` | benchmark.html 생성 | `[x]` 확인 (벤치마크 마크다운 출력) |
| `py main.py eval --compare-baseline` | precision/recall/F1 출력 | `[x]` 확인 (1.000/1.000/1.000) |
| `py main.py history --last 30d` | history 요약 출력 | `[x]` 확인 |
| `py main.py explain api-contract-sync` | 규칙 상세 설명 출력 | `[x]` 확인 |
| `py main.py init --preset api` | `.drift-gate.yml` 생성 | `[x]` 직접 실행 확인 (2026-05-21) |
| `py main.py report --out-html report.html --open` | HTML 생성 + 브라우저 열기 | `[x]` HTML 생성 확인 (2026-05-21) |

### 2-2. 핵심 정책 엔진 동작

검증 명령: `py -m pytest drift_gate/tests/ -q`

| 기능 | 테스트 위치 | 상태 |
|------|-------------|------|
| docs-only PR → pass (평가 생략) | `test_engine.py` | `[x]` 258 passed |
| test-only PR → pass | `test_engine.py` | `[x]` |
| BLOCKER + fail_on_blocker → fail | `test_engine.py` | `[x]` |
| drift-ignore reason 없는 BLOCKER → reject | `test_engine.py` | `[x]` |
| drift-ignore expires 만료 → reject | `test_engine.py` | `[x]` |
| CODEOWNERS approval 필요 → reject | `test_engine.py:315` | `[x]` |
| allow_ignore: false 규칙 → reject | `test_engine.py:294` | `[x]` |
| cross-file require 평가 | `test_engine.py::TestCrossFileRelations` | `[x]` |
| temporal gate (반복 ignore 경고) | `test_history.py:151` | `[x]` |

### 2-3. 아키텍처 경계 검증

core 내부에 I/O 코드가 없어야 함.

검증:
```
grep -r "print\|sys.stderr\|sys.exit" drift_gate/core/
```

- `drift_gate/core/policy/loader.py`: `[x]` `sys` import 없음, 경고를 `Policy.load_warnings[]`로 반환
- `drift_gate/core/` 나머지 파일: `[x]` grep 확인 완료 — 실제 print/sys 호출 없음, 주석·문자열 리터럴만 (2026-05-21)

### 2-4. Semantic Adapter 동작

Tree-sitter는 **미설치** 상태. 현재 동작은 regex heuristic.

- `drift_gate/adapters/ast/__init__.py`: `TREE_SITTER_AVAILABLE = False` — `[x]` 확인
- TypeScript/Python adapter: `[x]` heuristic 기반 동작 (`lru_cache` 포함)
- **Tree-sitter 실제 grammar 설치 후 동작**: `[ ]` 미완료 (교체 경로 주석만 있음)

---

## 3. 문서-코드 정합성

README에 적힌 내용이 실제 코드와 맞는지 확인하는 항목.

### 3-1. 확인된 일치

- `[x]` README flow diagram이 `engine.py → classifier → evaluator → gate` 흐름과 일치
- `[x]` 패키지 구조 다이어그램이 실제 디렉터리 구조와 일치
- `[x]` `drift-ignore` 파싱 규칙 표 (severity × reason 유무) — `evaluator.py` 로직과 일치

### 3-2. 확인 필요한 불일치 후보

| README 문구 | 코드 실제 상태 | 검증 방법 |
|-------------|----------------|-----------|
| "No historical tracking" 같은 오래된 한계 설명 | history 기능 구현됨 | README 전문 검색 |
| `docs-only` / `test-only` 분류 경로 패턴 | CLAUDE.md 표 vs `classifier.py` | `grep docs-only drift_gate/core/classification/classifier.py` |
| JSON 출력 계약 스키마 | `result.py::to_dict()` 실제 키셋 | 스키마 필드 대조 |

### 3-3. action.yml 문구

- `[x]` `action.yml`의 description 필드에 한글 mojibake 없음 — 전체 ASCII 영어 (2026-05-21)
- `[x]` `action.yml`의 input/output 이름이 실제 GitHub Action runner와 일치 — `runner.py` `_write_github_output()` 키 대조 완료 (2026-05-21)

---

## 4. 비용·성능 수치 검증

> "토큰 절감 수치가 실제인지" 확인. 코드 확인으로 가능한 것과 불가능한 것을 구분합니다.

### 4-1. 프롬프트 캐싱 구현 여부 (코드로 확인 가능)

- `[x]` `enricher.py:_call_api()` — `cache_control: {type: ephemeral}` 시스템 프롬프트에 적용
- `[x]` `anthropic-beta: prompt-caching-2024-07-31` 헤더 전송
- `[x]` `_SYSTEM_PROMPT` 상수 분리 (고정 텍스트)
- `[x]` user 메시지(`violations_summary`)가 요청마다 달라지는 가변 부분으로 분리

### 4-2. "50-70% 절감" 수치 검증 (코드만으로는 증명 불가)

이 수치는 실측값이 아닌 **추정값**입니다. 실제 절감을 확인하려면:

1. `anthropic.beta.messages` 응답의 `usage.cache_read_input_tokens` 필드 확인
2. cache hit / miss 케이스를 5회 이상 연속 호출하여 비교

현재 코드에서 `usage` 필드를 로깅하는 코드: `[x]` `EnrichmentMetrics.format_report()` 구현됨, `check` 명령에서 자동 출력

**결론**: 구조는 맞게 구현됐으나, "80%/60-70% 절감" 수치는 실측 미확인.

### 4-3. semantic signal 도입으로 FP 3→0 검증

- `[x]` benchmark 출력: path-only FP=3, patch-aware FP=0, F1 0.889→1.000
- `[x]` `drift_gate/tests/fixtures/` 20개 fixture로 재현 가능
- 재현 방법: `py main.py eval --compare-baseline`

### 4-4. 런타임 성능

- `[x]` demo 기준 path-only 3608 files/sec, patch-aware 7024 files/sec
- `[x]` 100+ changed files 실제 실행 검증 — `pr_large_100_files.json` fixture + `TestLargePR` (3 tests)

---

## 5. 코드 품질 리뷰

### 5-1. 테스트 커버리지

```
py -m pytest drift_gate/tests/ -q
```

- `[x]` 423 passed (2026-05-21 기준)
- `[x]` intensity classifier 단위 테스트: `test_intensity_classifier.py` (80 cases)
- `[x]` enrichment fallback 테스트: `test_enricher.py` (캐싱 payload 구조 포함)
- `[x]` GitHub API 페이지네이션: `test_github_client.py` (42/100/157/300/301 files)
- `[x]` HTML reporter 출력 형식 테스트 — `test_html_reporter.py` (50+ cases): 구조, XSS escape, token panel, temporal, violation card
- `[x]` `apply_temporal_gate()` + history 통합 경로 E2E 테스트 — `test_history.py::TestTemporalGateE2E` (6 tests)

### 5-2. 설계 검토 포인트

| 항목 | 코드 위치 | 현재 상태 |
|------|-----------|-----------|
| `glob_matcher.py` 불필요한 replace 제거됐는지 | `utils/glob_matcher.py:24-25` | `[x]` 불필요한 replace 없음 — `re.escape()` + 직접 파싱 사용 |
| GitHub API error 메시지에 HTTP 코드별 힌트 있는지 | `adapters/github/client.py` | `[x]` `_github_api_error()` + `_HTTP_HINTS` dict (401/403/404/429/500 등) 구현됨 |
| `generate_docs_draft()` fallback이 빈 dict가 아닌지 | `enricher.py:203` | `[x]` `_fallback_draft()` 구현됨 |
| Markdown/HTML/JSON이 동일한 `EvaluationResult`에서 파생되는지 | `reporters/` 전체 | `[x]` `MarkdownReporter`, `HtmlReporter`, `JsonReporter` 모두 `result.to_dict()` 또는 `result` 직접 사용 |

### 5-3. 보안·안전성

| 항목 | 상태 |
|------|------|
| fork PR에서 secret 미노출 (read 권한만 사용) | `[x]` `github_action/runner.py` 확인 |
| HTML 출력에서 user input HTML escape | `[x]` 구현됨 + XSS 버그 수정 (checklist copyText에서 `html.escape(json.dumps(item))`) — `test_html_reporter.py::TestHtmlEscape` |
| path traversal 방지 (`../` 포함 경로 거부) | `[x]` `_sanitize_path()` 구현됨 — `test_html_reporter.py::TestPathTraversal` |
| token이 로그에 노출되지 않는지 | `[x]` 확인됨 — `test_html_reporter.py::TestTokenNotLogged` |

---

## 6. 미완료 / 부분 구현

실제로 동작하지 않거나 scaffold만 있는 기능:

| 기능 | 현재 상태 | 다음 단계 |
|------|-----------|-----------|
| Tree-sitter 실제 grammar 기반 파싱 | `[x]` `tree-sitter-language-pack` grammar 기반 parse 경로 추가, `TREE_SITTER_AVAILABLE = True` 검증 | — |
| `usage.cache_read_input_tokens` 로깅 | `[x]` `enricher.py:_call_api()` usage 파싱 완료, `EnrichmentMetrics.format_report()` check 명령에서 출력 | — |
| MCP server 실제 패키징 | `[x]` `pyproject.toml`에 `drift-gate-mcp = "drift_gate.adapters.mcp.server:main"` entry point 추가 완료 | — |
| `drift-gate init --preset` 실행 후 파일 생성 확인 | `[x]` `py main.py init --preset api --policy /tmp/...` 직접 실행 확인 (2026-05-21) | — |
| `--out-html --open` 브라우저 자동 열기 | `[x]` `py main.py report --out-html /tmp/test-report.html` 파일 생성 확인 (2026-05-21). `--open` 은 실행 환경 의존 | — |
| benchmark regression CI 자동 fail | `[x]` `benchmark.yml` `--max-fp 0 --max-fn 0 --min-f1 0.95` + `_run_eval()` `sys.exit(1 if failed or gate_failures else 0)` 완료 | — |
| Go / Java / Kotlin / Ruby semantic adapter | `[x]` 전용 adapter 추가 (`go_adapter.py`, `java_kotlin_adapter.py`, `ruby_adapter.py`) + grammar/heuristic fallback 테스트 | — |
| history trend HTML에 그래프 | `[x]` `render_history_html()`에 Chart.js 라인 차트 추가 (Violations/Runs/Ignored, 2026-05-21) | — |
| 체크리스트-코드 의미 검증 | `[x]` `self-audit` 명령 구현됨 (`core/self_audit/matcher.py`, `adapters/docs/checklist.py`) | — |
| Claude 토큰 절감 실측 | `[x]` `EnrichmentMetrics` usage 파싱 + 비용 계산 + `format_report()` 구현됨 | — |
| README 구현 일치성 검증 | `[x]` `docs-check` 명령 구현됨 (`adapters/docs/readme_contract.py`) | — |
| 일반 코드 리뷰 | `[x]` `review` 명령 구현됨 (`core/review/heuristics.py`) | — |

---

## 7. Self-Audit / Implementation Review Gate 요구사항

> 목표: Drift Gate 자체 개발에 대해 "이전 버전 대비 지금 변경이 문서/체크리스트/비용/품질과 일치하는지"를 검증한다.
> 기존 contract drift gate와 분리한다. 기본 CI gate는 deterministic contract sync이고, self-audit은 로컬/PR 보조 리뷰 모드로 둔다.

### 7-1. `고쳐야할점.md` 체크박스 의미 검증

원하는 동작:

- [x] git diff 기준으로 변경된 코드 파일과 `고쳐야할점.md`의 `[x]` 항목을 연결한다.
- [x] 체크된 항목이 실제 코드 변경, 테스트 추가, 문서 갱신 중 최소 하나의 evidence를 가져야 한다.
- [x] evidence가 없으면 `checklist-code-mismatch` 경고를 낸다.
- [x] 새 코드 변경이 있는데 대응되는 체크리스트 항목이 없으면 `missing-progress-entry` 경고를 낸다.
- [x] 단순 문자열 매칭만 하지 않고 파일 경로, 함수/클래스 이름, 테스트명, rule id를 함께 사용한다.

예상 출력:

```json
{
  "self_audit": {
    "checklist_items": [
      {
        "text": "Claude 프롬프트 캐싱 적용",
        "checked": true,
        "evidence": [
          "drift_gate/adapters/claude/enricher.py",
          "drift_gate/tests/test_enricher.py"
        ],
        "status": "supported"
      }
    ],
    "warnings": []
  }
}
```

구현 방향:

- [x] `adapters/git`에서 base diff 파일 목록과 patch 수집
- [x] `adapters/docs/checklist.py` 추가: Markdown 체크박스 파서
- [x] `core/self_audit/` 추가: 체크 항목 ↔ diff evidence 매칭
- [x] `py main.py self-audit --checklist 고쳐야할점.md --base main`
- [x] HTML/Markdown report에 "Self Audit" 패널 추가 (`--out-html` 옵션)
- [x] 테스트 fixture: supported / unsupported / missing-progress-entry 3종

### 7-2. 토큰 절약 실측 / 비용 계산

원하는 동작:

- [x] Claude 응답의 `usage.input_tokens`, `usage.output_tokens`, `usage.cache_creation_input_tokens`, `usage.cache_read_input_tokens`를 파싱한다.
- [x] prompt caching 적용 전/후 추정 비용을 계산한다.
- [x] 캐시 히트율, 절약 토큰, 절약률을 리포트한다.
- [x] API 호출이 없을 때도 dry-run estimate를 제공한다. (`py main.py check --token-estimate`)

예상 출력:

```text
Claude token efficiency
- input_tokens: 820
- cache_creation_input_tokens: 210
- cache_read_input_tokens: 610
- estimated_uncached_input_tokens: 1430
- saved_input_tokens: 610
- estimated_savings: 42.7%
```

구현 방향:

- [x] `ClaudeEnricher`가 usage 메타데이터를 `EvaluationResult.enrichment_metrics`에 기록
- [x] 모델별 가격표는 코드 상수로 두되 날짜와 출처를 명시 (`result.py:8`)
- [x] `--json` 출력에 `enrichment_metrics` 포함
- [x] `test_enricher.py`에 usage/cost 계산 테스트 추가
- [x] 네트워크 없는 테스트에서는 fake response로 검증

주의:

- [x] 실제 가격은 변할 수 있으므로 "estimated"로 표시
- [x] API key/token 값은 절대 로그에 출력하지 않음

### 7-3. README / Docs 구현 일치성 검증

원하는 동작:

- [x] README에 적힌 CLI 명령이 실제 argparse에 존재하는지 확인한다.
- [x] README의 JSON schema 예시가 `EvaluationResult.to_dict()`와 맞는지 확인한다.
- [x] README의 policy field 설명이 `Policy.from_dict()` 모델과 맞는지 확인한다. (`policy-field-mismatch` 경고)
- `부분 구현` 문서에 "구현됨"이라고 쓰인 기능이 테스트 또는 코드 evidence를 가져야 한다. → `py main.py self-audit --checklist 고쳐야할점.md --base main`으로 커버됨. docs-check의 stale-limitation 경고와 함께 사용.
- [x] 문서에 오래된 한계/로드맵 문구가 남아 있으면 경고한다. (`stale-limitation` 경고)

예상 경고:

```text
docs-consistency warning
- README mentions `drift-gate self-audit`, but argparse command is missing.
- README JSON sample omits `temporal_warnings`, which exists in EvaluationResult.to_dict().
```

구현 방향:

- [x] `adapters/docs/readme_contract.py` 추가
- [x] argparse parser introspection으로 실제 command/option 목록 추출
- [x] dataclass `to_dict()` sample과 README JSON block 비교
- [x] `py main.py docs-check --docs README.md docs/**/*.md`
- [x] CI에서는 warn, release 전에는 fail 가능 옵션 제공 (`--fail-on-warn`)

### 7-4. 일반 코드 리뷰 모드

원하는 동작:

- [x] 기존 Drift Gate와 분리된 optional `review` 명령을 제공한다.
- [x] 정책 위반이 아니라 코드 품질/버그/테스트 누락/설계 위험을 찾는다.
- [x] findings는 severity, file, line, reason, suggested fix를 포함한다.
- [x] deterministic heuristic review를 우선하고, LLM은 optional enrichment로만 사용한다.
- [x] CI fail 기본값은 false. 사용자가 `--fail-on high`처럼 명시할 때만 실패 처리한다.

검사 항목 후보:

- [x] core 내부 I/O 금지 위반: `print`, `sys.exit`, `subprocess`, network import
- [x] adapter 외부 I/O 경계 위반 (`core-io-filesystem`: `open()`, `read_text()`, `os.path.*`)
- [x] 새 public function에 대응하는 테스트 없음 (`find_test_gaps()`, diff patch 기반)
- [x] 새 policy field에 validator 테스트 없음 → `find_test_gaps()`가 policy validator/model 변경 시 `drift_gate/tests/test_validator.py` 동반 변경 여부를 검사.
- [x] reporter에 새 field 표시 누락 → `find_reporter_field_gaps()`가 `EvaluationResult.to_dict()` 키와 Markdown/HTML renderer 출력 문자열을 비교.
- [x] mutable default / broad exception / swallowed exception / token logging

명령 예시:

```bash
py main.py review --base main --format markdown
py main.py review --base main --fail-on high
```

기대 출력 형식:

```md
## Drift Gate Code Review

### Findings

- HIGH `drift_gate/core/foo.py:42`
  core layer imports subprocess. Move I/O to adapters.

### Test Gaps

- `temporal_warnings` is serialized but HTML rendering has no test.
```

### 7-5. 이 기능들의 성공 기준

- [x] `py main.py self-audit --checklist 고쳐야할점.md --base main` 실행 가능
- [x] 체크된 항목 중 evidence 없는 항목을 최소 1개 fixture로 잡아냄
- [x] `py main.py check --anthropic-api-key ... --json`에서 token savings 표시 (또는 `--token-estimate`로 dry-run)
- [x] `py main.py docs-check`가 README command/schema drift를 잡아냄
- [x] `py main.py review --base main`이 일반 코드 리뷰 findings를 출력
- [x] 모든 기능은 LLM 없이 deterministic fallback 동작
- [x] LLM은 설명 보강만 수행하고 pass/fail 결정은 core/adapters의 deterministic evidence가 수행
- [x] 테스트 최소 20개 추가: checklist parser, evidence matcher, token metrics, docs-check, review heuristics (449 → 460 passed)

---

## 8. 빠른 실행 검증 스크립트

```bash
# 1. 테스트 전체 통과 확인
py -m pytest drift_gate/tests/ -q

# 2. benchmark 재현
py main.py eval --compare-baseline

# 3. CLI 명령 전체 smoke test
py main.py doctor
py main.py explain api-contract-sync
py main.py history --last 30d
py main.py demo

# 4. core I/O 오염 검사
grep -rn "print\|sys.stderr\|sys.exit" drift_gate/core/

# 5. tree-sitter 상태 확인
python -c "from drift_gate.adapters.ast import TREE_SITTER_AVAILABLE; print(TREE_SITTER_AVAILABLE)"
```

기대 결과 (2026-05-21 기준):
- `pytest`: `460 passed`
- `eval`: `candidate 20/20 | F1 1.000`
- `grep core/`: 주석·문자열 리터럴만 — 실제 I/O 호출 없음
- `TREE_SITTER_AVAILABLE`: `False`
